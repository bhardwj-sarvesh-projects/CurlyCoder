{
  "_comment": "Reference template — copy the 'hooks' object into your .qoder/settings.json or ~/.qoder/settings.json. Replace CODELEAN_DIR with the path to your CODELEAN checkout (e.g. ~/.qoder/plugins/CODELEAN or the npm global install path).",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node CODELEAN_DIR/hooks/CODELEAN-mode-tracker.js"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "task|Task",
        "hooks": [
          {
            "type": "command",
            "command": "node CODELEAN_DIR/hooks/CODELEAN-subagent.js"
          }
        ]
      }
    ]
  }
}
